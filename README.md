# Winows Message Sender

A small windows utility to send (initially PostMessage & SendMessage) custom windows messages (WM_USER + ?) to a known window handle. Used to support desktop automation tasks.

